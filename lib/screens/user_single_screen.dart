import 'package:flutter/material.dart';

class UserSingleScreen extends StatelessWidget {
  const UserSingleScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("View User"),
      ),
      body: const Center(
        child: Text(""),
      ),
    );
  }
}
